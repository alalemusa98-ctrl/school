# وثيقة المواصفات الفنية للنسخة الأولى (V1 - MVP)
## مشروع: نظام إدارة المدرسة المرتكز على الطالب (Libyan School Management System)

> **نوع الوثيقة:** مرجع تنفيذي للنسخة المبسطة الأدنى (Minimum Viable Product - MVP). هذه النسخة تركز على السرعة، الاستقرار، والبساطة، وتلغي جميع التعقيدات غير الضرورية في المرحلة الأولى.

---

## 1. نطاق وأهداف النسخة الأولى (V1 Scope & Objectives)

توفير نظام ويب لإدارة مدرسة أساسية في ليبيا (الصفوف من **1 إلى 9**) يهدف حصرًا إلى تسهيل العملية التعليمية للطالب والمعلم والإدارة بأسلوب بسيط ومباشر.

### الأهداف الرئيسية لـ V1:
1. **بساطة مطلق:** استبعاد منطق الفترات الدراسية (الترم) والجدولة الزمنية المعقدة لتسريع عملية البناء والتشغيل.
2. **العرض فقط للطالب:** الطالب يطلع على واجباته، امتحاناته، وجدوله التحصيلي بدون رفع أو تسليم إلكتروني.
3. **مهمة لشعبة واحدة:** كل واجب أو امتحان يُنشأ لشعبة دراسية واحدة فقط لتسهيل المعالجة وقواعد البيانات.

---

## 2. الهيكلية التعليمية في V1 (Educational Structure)

```
المدرسة (School)
  └── السنة الدراسية (Academic Year)
        └── الصفوف الدراسية (Grades) [الصف 1 إلى 9]
              ├── المواد الدراسية (Subjects) [ثابتة وموحدة لجميع شعب الصف]
              └── الشعب / القاعات (Sections / Halls) [مثل 5أ، 5ب، 5ج]
                    ├── قائمة الطلاب (Students)
                    └── الجدول الدراسي الأسبوعي (Weekly Schedule)
```

---

## 3. أدوار المستخدمين وصلاحياتهم في V1 (User Roles & Capabilities)

### 3.1 الإدارة (Admin)
* **دخول:** اسم مستخدم + كلمة مرور إدارية.
* **إدارة الهيكل:**
  1. إضافة وتعديل الصفوف الدراسية (من 1 إلى 9).
  2. إضافة وتعديل الشعب/القاعات لكل صف (مثل 5أ، 5ب).
  3. إضافة وتعديل المواد الدراسية وتحديدها لكل صف (جميع شعب الصف تدرس نفس المواد).
* **إدارة الطلاب المعلمين:**
  1. إضافة طلاب وتعيينهم لشعبة، وتوليد `رقم الجلوس` و `كود الطالب`.
  2. تعديل شعبة الطالب (نقل مباشر محلي).
  3. إضافة المعلمين وتكليف كل معلم بالمادة والشعب التي يدرسها.
* **إدارة الجداول:**
  1. تحديد عدد الحصص اليومية.
  2. بناء الجدول الأسبوعي لكل شعبة (توزيع الحصص: اليوم، الحصة، المادة، المعلم).

### 3.2 المعلم (Teacher)
* **دخول:** اسم مستخدم + كلمة مرور.
* **النطاق:** يرى فقط المواد والشعب المخصصة له من الإدارة.
* **إدارة المحتوى التعليمي:**
  1. إنشاء واجب أو امتحان لشعبة واحدة محددة.
  2. رفع ملف مرفق (PDF / صورة) أو كتابة وصف نصي.
  3. تعديل أو حذف الواجبات والامتحانات التي أنشأها.
* **الحل النموذجي (مباشر وبسيط):**
  * خيار إرفاق "حل نموذجي" (ملف أو نص) أثناء الإنشاء أو لاحقاً.
  * **المنطق:** إذا رُفع الحل النموذجي -> يظهر زر "عرض الحل" للطالب فوراً. إذا لم يُرفع -> لا يظهر أي شيء.

### 3.3 الطالب (Student)
* **دخول:** `رقم الجلوس` + `كود الطالب`.
* **الصلاحية:** للعرض فقط (Read-Only).
* **الواجهات المتاحة:**
  1. **الواجبات (Homeworks):** عرض جميع الواجبات المطلوبة لشعبته مرتبة من الأحدث للأقدَم، مع زر "عرض الحل" في حال وجود حل مرفق من المعلم.
  2. **الامتحانات (Exams):** عرض جميع الامتحانات الخاصة بشعبته مع إمكانية تحميل ملف الامتحان أو الحل إن وجد.
  3. **الجدول الدراسي (Schedule):** عرض جدول حصص شعبته الأسبوعي بشكل واضح ومبسط.
  4. **المواد الدراسية (Subjects):** تصفح مواد الصف، وعند النقر على مادة تُصفّى الواجبات والامتحانات التابعة لهذه المادة فقط.

---

## 4. نموذج بيانات النسخة الأولى (V1 Database Schema)

تصميم بسيط جداً ومباشر وقابل للربط والتوسع في المستقبل:

```sql
-- 1. السنوات الدراسية
AcademicYears (
    id PRIMARY KEY,
    name VARCHAR -- مثل: 2025/2026
);

-- 2. الصفوف الدراسية (1 إلى 9)
Grades (
    id PRIMARY KEY,
    level INT,
    name VARCHAR -- الصف الأول، الصف الخامس...
);

-- 3. الشعب / القاعات
Sections (
    id PRIMARY KEY,
    grade_id FOREIGN KEY -> Grades,
    name VARCHAR -- 5أ، 5ب...
);

-- 4. المواد الدراسية
Subjects (
    id PRIMARY KEY,
    grade_id FOREIGN KEY -> Grades,
    name VARCHAR
);

-- 5. الطلاب
Students (
    id PRIMARY KEY,
    roll_number VARCHAR UNIQUE, -- رقم الجلوس
    student_code VARCHAR, -- كود الدخول
    full_name VARCHAR,
    grade_id FOREIGN KEY -> Grades,
    section_id FOREIGN KEY -> Sections
);

-- 6. المعلمون
Teachers (
    id PRIMARY KEY,
    username VARCHAR UNIQUE,
    password_hash VARCHAR,
    full_name VARCHAR
);

-- 7. تكليف المعلمين (مادة + شعبة)
TeacherAssignments (
    id PRIMARY KEY,
    teacher_id FOREIGN KEY -> Teachers,
    subject_id FOREIGN KEY -> Subjects,
    section_id FOREIGN KEY -> Sections
);

-- 8. الواجبات والامتحانات (مباشرة لشعبة واحدة)
AssessmentTasks (
    id PRIMARY KEY,
    title VARCHAR,
    description TEXT,
    task_type ENUM('HOMEWORK', 'EXAM'),
    subject_id FOREIGN KEY -> Subjects,
    section_id FOREIGN KEY -> Sections, -- شعبة واحدة فقط في V1
    teacher_id FOREIGN KEY -> Teachers,
    attachment_path VARCHAR, -- ملف الواجب/الامتحان
    due_date DATE NULL,
    created_at DATETIME,
    
    -- الحل النموذجي المبسط
    has_solution BOOLEAN DEFAULT false,
    solution_text TEXT NULL,
    solution_attachment_path VARCHAR NULL
);

-- 9. الجدول الدراسي الأسبوعي
ScheduleSlots (
    id PRIMARY KEY,
    section_id FOREIGN KEY -> Sections,
    day_of_week INT, -- 1 إلى 7
    slot_number INT, -- رقم الحصة
    subject_id FOREIGN KEY -> Subjects,
    teacher_id FOREIGN KEY -> Teachers
);
```

---

## 5. مميزات تم استبعادها من V1 وتم تأجيلها للنسخة V2

1. ❌ **نظام الفترات الدراسية (Active Period / الترم):** تم الاستغناء عنه بالاعتماد على العرض الزمني المباشر في V1.
2. ❌ **جدولة إظهار الحل النموذجي برابط زمني:** تم استبداله بـ (وجود/عدم وجود الحل) المباشر.
3. ❌ **استهداف عدة شعب بمهام واحدة:** تم التبسيط إلى مهمة واحدة لكل شعبة.
4. ❌ **سجل الأرشيف التراكمي لنقل الطالب:** تم التبسيط بنقل الطالب المباشر لشعبته الجديدة.
