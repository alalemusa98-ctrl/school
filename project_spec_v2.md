# وثيقة المواصفات الفنية للنسخة الثانية المتقدمة (V2 Roadmap)
## مشروع: نظام إدارة المدرسة المرتكز على الطالب (Libyan School Management System)

> **نوع الوثيقة:** خريطة الطريق والتوسعة المستقبلية (Advanced Enhancements & Feature Expansion). هذه الوثيقة تبني على قاعدة بيانات النسخة الأولى V1 وتضيف الميزات الذكية المتقدمة بعد استقرار V1 في البيئة الميدانية.

---

## 1. أهداف وخريطة طريق النسخة الثانية (V2 Roadmap Goals)

ترقية النسخة الأولى العادية إلى نظام إلكتروني ذكي ومؤتمت بالكامل يراعي الدورة الزمنية للسنة الدراسية والتحكم في إتاحة المحتوى.

---

## 2. الميزات المتقدمة المضافة في V2 (Advanced Features)

### 2.1 الفترات الدراسية والأرشفة الذكية (Active Periods & Auto-Archiving)
* **المفهوم:** تقسيم السنة الدراسية إلى فترات (`الفترة الأولى` / `الفترة الثانية` / `الفترة الثالثة`).
* **آلية العمل:**
  * تُفعّل الإدارة فترة دراسية واحدة كـ **"الفترة الحالية النشطة" (Active Period)**.
  * أي واجب أو امتحان يُنشئه المعلم يُربط تلقائياً بالفترة النشطة.
  * في واجهة الطالب: يُعرض فقط محتوى "الفترة الحالية" في الصفحة الرئيسية.
  * عند انتقال الإدارة للترم الثاني وتفعيل "الفترة الثانية"، تنتقل جميع واجبات وامتحانات الفترة الأولى تلقائياً إلى صفحة **"الواجبات/الامتحانات السابقة (الأرشيف)"**.

### 2.2 التحكم والجدولة الزمنية للحلول النموذجية (Scheduled Model Answer Release)
* **المفهوم:** منع ظهور الحل النموذجي إلا في موعد محدد سلفاً من قِبل المعلم.
* **آلية العمل:**
  * يحدد المعلم خيار الظهور:
    1. **فوري (Immediate):** متاح بمجرد الرفع.
    2. **مجدول زمنياً (Scheduled):** إدخال `تاريخ ووقت مستقبلي` (مثلاً: الجمعة الساعة 6 مساءً).
  * **قاعدة الحجب الصارمة (Strict Security Lock):** لا يظهر زر أو دلالة على وجود الحل النموذجي للطالب حتى يحين موعد الجدولة بالظبط.

### 2.3 نشر الواجبات لعدة شعب دفعة واحدة (Multi-Section Task Broadcasting)
* **المفهوم:** توفير وقت المعلم عند تدريسه لنفس المادة لأكثر من شعبة (مثل 5أ، 5ب، 5ج).
* **آلية العمل:**
  * عند إنشاء واجب أو امتحان، يستطيع المعلم تحديد عدة شعب عبر مربعات اختيار (Checkboxes).
  * يُحفظ الواجب مرة واحدة ويُرتب بعدة شعب في قاعدة البيانات، ويستطيع المعلم تعديله لجميع الشعب دفعة واحدة.

### 2.4 نقل الطلاب الذكي وسجل الأرشيف التراكمي (Smart Transfer & Student History)
* **المفهوم:** حماية أرشيف الطالب عند تغييره لشعبته.
* **آلية العمل:**
  * عند نقل طالب من شعبة (5أ) إلى شعبة (5ج) منتصف السنة، تتغير واجهته ومواده الحالية لتطابق 5ج.
  * يحتفظ الطالب بإمكانية الدخول لأرشيف الواجبات والامتحانات التي أُنشئت له سابقاً عندما كان ينتمي لشعبة 5أ، مع استعراض الحلول النموذجية المتاحة لها.

---

## 3. التحديثات المضافة على قاعدة البيانات (V2 Schema Migration Delta)

للتحويل من قاعدة بيانات V1 إلى V2 بدون فقدان البيانات، يتم تطبيق التعديلات التالية:

```sql
-- 1. إضافة جدول الفترات الدراسية
CREATE TABLE AcademicPeriods (
    id PRIMARY KEY,
    academic_year_id FOREIGN KEY -> AcademicYears,
    name VARCHAR, -- الفترة الأولى، الفترة الثانية...
    is_active BOOLEAN DEFAULT false
);

-- 2. ترقية جدول المهام AssessmentTasks
ALTER TABLE AssessmentTasks 
ADD COLUMN period_id FOREIGN KEY -> AcademicPeriods,
DROP COLUMN section_id; -- إلغاء شعبة واحدة مفردة والاستعانة بالجدول المتقاطع

-- 3. جدول الربط المتعدد بين الواجبات والشعب
CREATE TABLE TaskSections (
    task_id FOREIGN KEY -> AssessmentTasks,
    section_id FOREIGN KEY -> Sections,
    PRIMARY KEY (task_id, section_id)
);

-- 4. جدول الحلول النموذجية المتقدم المجدول
CREATE TABLE ModelAnswers (
    id PRIMARY KEY,
    task_id FOREIGN KEY -> AssessmentTasks UNIQUE,
    answer_text TEXT,
    attachment_path VARCHAR,
    visibility_type ENUM('IMMEDIATE', 'SCHEDULED'),
    publish_at DATETIME NULL -- تاريخ ووقت الجدولة
);

-- 5. جدول سجل تنقلات الطلاب (للحفاظ على أرشيف الشعب السابقة)
CREATE TABLE StudentSectionHistory (
    id PRIMARY KEY,
    student_id FOREIGN KEY -> Students,
    section_id FOREIGN KEY -> Sections,
    assigned_at DATETIME,
    removed_at DATETIME NULL
);
```

---

## 4. ملخص مسار الانتقال والتوسعة (Upgrade Path)

```
+------------------------------------+
|       النسخة الأولى V1 (MVP)       |
|  - إطلاق سريع واستقرار تام        |
|  - واجبات وجداول وتصفح بسيط        |
+------------------------------------+
                  │
                  ▼
+------------------------------------+
|     الترقية إلى النسخة V2          |
|  - تفعيل جدول الفترات والأرشيف     |
|  - إضافة جدولة الحلول وقواعد الوقت |
|  - استهداف متعدد للشعب في المهمة   |
+------------------------------------+
```
